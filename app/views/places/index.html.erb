<% if @current_user %>

<h3 class="display-4 mb-4">My Places</h3>

  <p>
    <a class="btn btn-primary btn-sm mt-2" href="/places/new">Add a New Place</a>
  </p>
  
  <ul>
    <% for place in @places %>
      <li><a href="/places/<%= place["id"] %>"><%= place["name"] %></a></li>
    <% end %>
  </ul>

<% else %>
  Please log in to continue
<% end %>