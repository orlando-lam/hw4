<!DOCTYPE html>
<html>
  <head>
    <title>Travel Journal</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css">
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Travel Journal</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="btn btn-default">
            <a class="nav-link" aria-current="page" href="/">Home</a>
          </li>
        </ul>

        <ul class="navbar-nav mb-2 mb-lg-0">
          <% if @current_user = User.find_by({ "id" => session["user_id"]}) %>
            <li class="navbar-text align-self-center">Logged in as: <%= @current_user["username"] %></li>
            <li class="btn btn-default"><a href="/logout" class="nav-link">Logout</a></li>
          <% else %>
            <li class="btn btn-default"><a href="/login" class="nav-link">Login</a></li>
            <li class="btn btn-default"><a href="/users/new" class="nav-link">Sign Up</a></li>
          <% end %>
        </ul>  
        </div>
      </div>
    </nav>

    <div class="container pt-4">  
      <div class="col text-center">
        <%= flash["notice"] %>
      </div>      
      <%= yield %>
    </div>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
    </div>
  </body>

</html>
